export enum Operaor {
  ADD = '+',
  MINUS = '-',
  MULTIPLICATOR = '*',
}

export enum MeterType {
  DERIVED = 'derived',
  MEASUREMENT = 'measurement',
}

export enum MeterPurpose {
  CONSUMER = 'consumer',
  AUDIT = 'audit',
  BULK = 'bulk',
  PUBLIC_POWER = 'public_power',
  GENERATOR_POWER = 'generator_power',
}
